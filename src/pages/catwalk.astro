---
import { CatwalkApp } from '../components/catwalk/CatwalkApp'
import SiteLayout from '../layouts/SiteLayout.astro'
---

<SiteLayout
  title="CATWALK — the MIAW PRIX rotation"
  description="Every coin in the MIAW PRIX rotation: who walks the runway, who is in the line-up, and which slots are open."
>
  <CatwalkApp endpoint="/api/agent-arena" client:load />
</SiteLayout>
