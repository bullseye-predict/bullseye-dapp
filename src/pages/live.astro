---
import { PredictionApp } from '../components/prediction/PredictionApp'
import SiteLayout from '../layouts/SiteLayout.astro'
---

<SiteLayout title="Live Arena · SOLZ / ODDS" description="Watch SOLZ matches and connect a Solana wallet for SOL and SOLZ prediction rails.">
  <PredictionApp environmentId={String(import.meta.env.VITE_DYNAMIC_ENVIRONMENT_ID ?? '').trim()} apiUrl={String(import.meta.env.PUBLIC_PREDICTION_API_URL ?? '').trim()} client:only="react" />
</SiteLayout>
